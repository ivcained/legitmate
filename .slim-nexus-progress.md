# LegitMate NEXUS-Sprint Progress

## Phase 0 — audit

Status: complete, gate verdict NEEDS WORK.

Evidence:
- `/root/legitmate` is clean and synchronized with `origin/main` at the current remote revision.
- `npm test`, `npm run lint`, `npm run typecheck`, and `npm run build` pass.
- The current product is a provider-free local lifecycle demo, not yet a real-user MVP.
- No Graph integration, Graph SDK, live provider query, persistence, authentication, or browser end-to-end test exists.
- The UI currently creates a static YouTube preset without using the brief to derive configuration.

## Phase 1 — scope lock

Status: locked; implementation lanes active.

Product Manager decision: ship a commissioning-to-research-trial MVP. The Graph live research is the only sponsor integration in the critical path. ENSv2 is conditional and must not jeopardize the core flow.

Locked vertical slice for the next increment:

1. Submit a brief.
2. Persist a versioned local workspace record.
3. Derive a prepared YouTube configuration from the brief using a deterministic server-owned parser.
4. Review requested permissions with explicit per-permission approve/deny decisions.
5. Complete a clearly marked local sandbox entitlement/provisioning step.
6. Run a deterministic trial whose output is derived from the brief/configuration.
7. Require separate activation acknowledgement and approval.
8. Render an attributable audit timeline from recorded events.
9. Add a browser-level test for the happy path and blocked activation.

Out of scope for this increment:
- Real customer billing.
- Real external workspace provisioning.
- Arbitrary account connections, publishing, messaging, or purchases.
- Sponsor integrations before the core flow is real and tested.

## Evidence gates

Gate 1: unit/domain tests cover parser, permission decisions, lifecycle invariants, persistence serialization, and audit events.

Gate 2: browser test completes the flow from a clean state and proves activation is blocked until approval.

Gate 3: lint, typecheck, test, build, and clean public repository state pass in the exact worktree that is pushed.

Gate 4: Reality Checker and Evidence Collector review the changed build. Do not call this a real-user MVP until persistence/auth/provider boundaries are explicitly disclosed.

## Next ownership lanes

- Backend Architect: domain parser, permission decisions, persistence model, deterministic trial.
- Frontend Developer + UX Architect: connect UI to one workflow aggregate and expose real decisions.
- DevOps Automator: browser test harness and reproducible local run.
- Evidence Collector + Reality Checker: read-only gate after implementation.
- Growth team remains deferred until a usable public flow and five user reactions exist.
