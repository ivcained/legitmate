# Live Graph provider access (server-only; required for Graph prize qualification)
GRAPH_API_KEY=
GRAPH_SUBGRAPH_ID=

# Demo mode remains available without Graph credentials.
NEXT_PUBLIC_DEMO_MODE=true
