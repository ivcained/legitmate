# ETHGlobal Final Execution Board

## Current truth

LegitMate is a deployed, tested Agent37-backed Hermes commissioning product with Privy client onboarding and a live Graph adapter. Prize qualification is not complete until live provider credentials, real testnet transactions, and evidence artifacts exist.

## Order

1. Restore a deterministic, green GitHub-to-VPS deployment.
2. Reconcile and commit the Orgo-inspired Hermes catalog and Privy wallet/approval UI changes only after source review.
3. Configure Privy app ID and server credentials outside Git; verify embedded wallet login.
4. Implement one real Privy B2B approval-controlled testnet transfer.
5. Integrate the live Graph response into a user-visible research result.
6. Run Reality Checker, Evidence Collector, browser QA, and clean-checkout verification.
7. Freeze scope and produce the submission evidence pack/video.

## Prize decision

Primary: The Graph From Scratch, only after live Graph data materially affects a decision/output.

Secondary: Privy B2B financial product and Best financial flow, only after one real wallet, one real control, and one confirmed testnet financial flow.

Do not spend the critical path on Arc, Chainlink, ENSv2, or additional templates until these three proof points exist.

## Gates

- Deployment green on the exact pushed SHA.
- No secrets, generated agent files, or local environments committed.
- Client bundle contains no server secrets.
- Privy login and wallet creation are reproducible.
- Approval cannot be bypassed by the client.
- Transaction recipient, token, chain, amount, policy, and receipt are independently verifiable.
- Graph data is live, timestamped, and materially used.
- Demo states and limitations match the actual implementation.
